define([
	"backbone"

], function (
        Backbone
) {
    "use strict";


    var ProductsSelectionModel = Backbone.Model.extend({
        defaults: {

        }
    });


    return ProductsSelectionModel;
});